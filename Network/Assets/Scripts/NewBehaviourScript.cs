using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour {

    public GameObject k;

	// Use this for initialization
	/*void Start () {
        k = GameObject.FindGameObjectWithTag("Player");
	}
	
	// Update is called once per frame
	void Update () {
        if (k == null) k = GameObject.FindGameObjectWithTag("Player");
        else {
            transform.position = k.transform.position + k.transform.forward * -3 + new Vector3(0,4,0);
            transform.forward = k.transform.forward + new Vector3(0,-0.5f,0);
        };
    }*/
}
